export enum SyncStatus {
  SYNCED = "synced",
  PENDING = "pending",
  FAILED = "failed"
}

export enum BehaviorDomain {
  FITNESS = "fitness",
  ACADEMIC = "academic",
  NUTRITION = "nutrition"
}

export enum BehaviorAction {
  DONE = "done",
  SKIPPED = "skipped",
  SUBMITTED = "submitted",
  MISSED = "missed"
}

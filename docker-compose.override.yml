# Local development overrides — NOT for production use.
# docker compose up will automatically merge this file.
services:
  backend:
    build:
      target: development
    environment:
      NODE_ENV: development
    volumes:
      - ./gak-backend:/app
      - /app/node_modules
